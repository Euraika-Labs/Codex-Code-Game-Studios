interface:
  display_name: "Create Epics"
  short_description: "Translate approved GDDs + architecture into epics — one epic per…"
  default_prompt: "Use $create-epics to translate approved GDDs + architecture into epics — one epic per architectural module. Defines scope, governing ADRs, engine risk, and untraced requirements. Does NOT break into stories — run $create-stories [epic-slug] after each epic is created."
policy:
  allow_implicit_invocation: false
