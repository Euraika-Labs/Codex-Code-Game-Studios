interface:
  display_name: "Test Setup"
  short_description: "Scaffold the test framework and CI/CD pipeline for the project's…"
  default_prompt: "Use $test-setup to scaffold the test framework and CI/CD pipeline for the project's engine. Creates the tests/ directory structure, engine-specific test runner configuration, and GitHub Actions workflow. Run once during Technical Setup phase before the first sprint begins."
policy:
  allow_implicit_invocation: false
