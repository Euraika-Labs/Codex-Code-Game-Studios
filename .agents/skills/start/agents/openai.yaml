interface:
  display_name: "Start"
  short_description: "First-time onboarding — asks where you are, then guides you to the…"
  default_prompt: "Use $start to first-time onboarding — asks where you are, then guides you to the right workflow. No assumptions."
policy:
  allow_implicit_invocation: true
