interface:
  display_name: "Art Bible"
  short_description: "Guided, section-by-section Art Bible authoring. Creates the visual…"
  default_prompt: "Use $art-bible to guided, section-by-section Art Bible authoring. Creates the visual identity specification that gates all asset production. Run after $brainstorm is approved and before $map-systems or any GDD authoring begins."
policy:
  allow_implicit_invocation: false
