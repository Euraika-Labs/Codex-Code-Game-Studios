interface:
  display_name: "Onboard"
  short_description: "Generates a contextual onboarding document for a new contributor or…"
  default_prompt: "Use $onboard to generates a contextual onboarding document for a new contributor or agent joining the project. Summarizes project state, architecture, conventions, and current priorities relevant to the specified role or area."
policy:
  allow_implicit_invocation: false
