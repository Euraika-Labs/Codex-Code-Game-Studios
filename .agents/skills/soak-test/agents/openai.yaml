interface:
  display_name: "Soak Test"
  short_description: "Generate a soak test protocol for extended play sessions. Defines what…"
  default_prompt: "Use $soak-test to generate a soak test protocol for extended play sessions. Defines what to observe, measure, and log during long play sessions to surface slow leaks, fatigue effects, and edge cases that only appear after sustained play. Primarily used in Polish and Release phases."
policy:
  allow_implicit_invocation: false
