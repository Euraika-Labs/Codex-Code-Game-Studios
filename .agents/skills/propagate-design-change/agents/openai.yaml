interface:
  display_name: "Propagate Design Change"
  short_description: "When a GDD is revised, scans all ADRs and the traceability index to…"
  default_prompt: "Use $propagate-design-change to when a GDD is revised, scans all ADRs and the traceability index to identify which architectural decisions are now potentially stale. Produces a change impact report and guides the user through resolution."
policy:
  allow_implicit_invocation: false
